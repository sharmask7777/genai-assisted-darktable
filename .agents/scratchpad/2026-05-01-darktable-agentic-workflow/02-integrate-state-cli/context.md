# Context: 02-integrate-state-cli

## Requirements
1. Add `init-session` command to `dt_ai/main.py`.
2. Input: `image_dir`.
3. Output: Confirmation of resumed or new session.
4. Integrate with `load_state` to ensure progress is tracked.
5. Provide clear feedback for the Agent to relay to the user.

## Implementation Paths
- `dt_ai/main.py`: Adding the new CLI command.
