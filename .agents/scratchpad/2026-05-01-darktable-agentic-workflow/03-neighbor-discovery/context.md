# Context: 03-neighbor-discovery

## Requirements
1. Implement `get_neighboring_files(target_path, count=2)` in `dt_ai/discovery.py`.
2. Discover files in the same directory, sorted alphabetically.
3. Return up to `count` files before and `count` files after.
4. Handle boundary cases (start/end of list).
5. Return absolute paths.

## Implementation Paths
- `dt_ai/discovery.py`: Adding neighbor-aware discovery.
