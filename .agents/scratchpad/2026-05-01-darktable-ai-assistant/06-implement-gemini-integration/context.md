# Context: 06-implement-gemini-integration

## Requirements
1. Create `dt_ai/ai.py`.
2. Implement `analyze_image(preview_path, prompt)` using `gemini-cli`.
3. Handle `gemini-cli` availability and errors.
4. Return AI response as a string.

## Implementation Paths
- `dt_ai/ai.py`: Core AI integration logic.
