# Context: 12-hex-encoding-utility

## Requirements
1. Implement `encode_params(values: List[float]) -> str` in `dt_ai/xmp.py`.
2. Use Python's `struct` module for little-endian float packing (`<`).
3. Convert binary data to lowercase hex strings.
4. Support multiple float values in a single call.

## Implementation Paths
- `dt_ai/xmp.py`: Adding binary encoding utilities.
