# Context: 09-implement-xmp-skeleton

## Requirements
1. Create `dt_ai/xmp.py`.
2. Generate valid Darktable XMP skeleton with namespaces (`x`, `rdf`, `darktable`).
3. Include core tags: `darktable:internal_version`, `darktable:history_end`, `darktable:history`.
4. Implement XML writing with proper header (`<?xpacket ...?>`).

## Implementation Paths
- `dt_ai/xmp.py`: Core XMP manipulation logic.
